// Package hardware detects local CPU, memory, and accelerator capabilities for BitCLI.
package hardware

// ARM64 support is handled through runtime.GOARCH in detector.go and shared platform probes.

